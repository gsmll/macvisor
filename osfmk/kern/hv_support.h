/* Recreated from kernelcache.arm64.kc (xnu-12377.121.10 RELEASE_ARM64_T8142, image base fffffe0007004000). Ground truth: Ghidra FUN_ names + addresses; all names are estimates. */

/*
 * hv_support.h (kern glue) — interface between the hypervisor subsystem
 * and the rest of the kernel (est.).
 *
 * Shared kernel dependencies are declared as externals here with one-line
 * notes; they are NOT recreated.
 */
