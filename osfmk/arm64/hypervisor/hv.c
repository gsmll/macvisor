/* Recreated from kernelcache.arm64.kc (xnu-12377.121.10 RELEASE_ARM64_T8142, image base fffffe0007004000). Ground truth: Ghidra FUN_ names + addresses; all names are estimates. */

/*
 * hv.c — Hypervisor.framework trap handlers (mach traps).
 *
 * Owned by the trap-dispatch tree.
 * Seed roots: FUN_fffffe000be39fd0 (est. hv_vmm_present).
 * Remaining hv trap handlers are found via the mach trap table record
 * handlers (see docs/chain-map.md), not by name (hv_vm_create literals are
 * absent from the binary).
 */
