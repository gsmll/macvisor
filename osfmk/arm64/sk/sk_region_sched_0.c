/* Recreated from exclavecore_bundle.t8142.RELEASE.im4p (cL4 Secure Kernel, GL1, arm64e, image
 * base 0) — the cL4 microkernel (cL4 (679.100.61)), seL4-derived. Ground truth: Ghidra FUN_ names
 * + addresses. All names are estimates unless string-matched.
 * Region 0: 0x180000-0x190000 — scheduler/thread machinery. */

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>

#include "sk_internal.h"

/* ------------------------------------------------------------------ *
 * Out-of-range cL4 helper declarations (extern; bodies reconstructed by
 * the sibling range workers that own them). Names are estimates.
 * ------------------------------------------------------------------ */
extern uint64_t FUN_001807dc(uint64_t a, uint64_t b, uint64_t c);  /* 0x1807dc in-range */

/* ================================================================== *
 * Functions in ADDRESS ORDER (0x180000-0x190000)
 * ================================================================== */
